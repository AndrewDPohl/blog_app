<!DOCTYPE html>
<html>
	<body>
		<h1>POSTS index</h1>
		<% postsList.forEach(function(posts) { %>
		<div>
  			<%= post.title %>
  		<div>
    		<%= post.content %>
  		</div>
	</div>
	<% }) %>
	</body>
</html>